# Oracle signer configuration

`oracle-signers.json` is generated locally by `node scripts/configure-oracle-wallets.mjs`
after fresh signer keys are configured. It is intentionally ignored until the final
production signer set is selected and independently distributed.
